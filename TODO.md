# TODO: Remove Falling Leaves Effect

- [x] Remove the call to `createFallingLeaves()` from the `DOMContentLoaded` event listener in `static/animations.js`
- [x] Remove the falling leaves HTML markup from `app.py`
